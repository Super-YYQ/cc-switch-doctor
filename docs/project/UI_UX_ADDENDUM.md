# CC Switch Doctor UI/UX 美化补充规范（Addendum v1.0）

> 用途：本补充规范用于覆盖并增强《CC Switch Doctor 项目需求与单目标交付设计文档 v1.1 审查修订版》中关于前端界面、交互体验与视觉风格的要求。若与旧版文档存在冲突，以本补充规范为准。

---

## 1. 目标

当前原型已基本实现功能，但界面存在以下问题：

- 视觉风格偏“开发调试页”，不够产品化；
- 顶部信息层级混乱，重点不突出；
- 左侧列表行高、字段排布和换行策略不佳；
- 右侧诊断结果像原始日志堆叠，阅读成本高；
- 按钮视觉重量接近，主次不清；
- 字体、颜色、圆角、间距不统一；
- “尝试链 / 日志 / 建议”未做好信息折叠与结构化展示；
- 与 CC Switch 主产品风格不统一，缺少“同家族 Companion Tool”的感觉。

本次 UI/UX 目标不是“做一个炫酷页面”，而是：

1. **明显提升审美与可读性**；
2. **保留桌面工具的专业感与轻量感**；
3. **风格接近 CC Switch**，让用户感觉这是同一生态下的配套工具；
4. **减少原始日志感**，加强诊断结论与建议的可读性；
5. **让 AI 一次 Goal 直接交付一个可用且不丑的产品界面**。

---

## 2. 总体设计方向

### 2.1 风格基调

整体采用：

- **CC Switch 风格借鉴**；
- 轻量、现代、柔和、清晰；
- 桌面工具感，而不是网页后台管理系统；
- 低对比大面积留白 + 清晰边界；
- 用浅色系作为首发默认主题；
- 保留暗色主题能力，但 **v0.1.0 可先高质量完成浅色主题**。

### 2.2 关键词

```text
Clean / Calm / Productized / Structured / Readable / Companion-to-CCS
```

### 2.3 不允许的视觉结果

- 不允许出现“像内部调试工具”的粗糙界面；
- 不允许大面积默认浏览器风格按钮；
- 不允许结果区直接堆原始文本大段输出；
- 不允许 URL、模型、错误说明乱换行导致视觉断裂；
- 不允许控件尺寸、圆角、边距不统一；
- 不允许色彩语义混乱（成功、警告、失败颜色必须一致）；
- 不允许把所有信息都放在一个滚动大面板里。

---

## 3. 视觉参考基线

### 3.1 参考对象

前端视觉与交互应明确参考：

1. **CC Switch 现有桌面端 UI 风格**（首要参考）；
2. 现代桌面工具常见布局：Linear / Raycast / Arc 风格的轻量信息层级；
3. 保持 Tauri + React 桌面应用的自然观感。

### 3.2 与 CC Switch 对齐的内容

尽量对齐以下风格元素：

- 胶囊标签（badge / chip）
- 分段筛选按钮（tab / segmented control）
- 卡片式内容容器
- 柔和边框和轻阴影
- 主次按钮风格
- 顶部工具栏的布局节奏
- 语义色体系（成功 / 警告 / 错误 / 中性）
- 字体字号层级
- 列表 Hover / Active / Selected 状态

### 3.3 不要求的内容

- 不要求像素级 1:1 复刻 CC Switch；
- 不要求复制其图标、资源文件或内部实现；
- 只要求 **视觉语言一致、家族感一致**。

---

## 4. 信息架构重构要求

## 4.1 页面总体布局

推荐使用 **三段式结构**：

```text
┌────────────────────────────────────────────┐
│ 顶部状态栏 / 工具栏                         │
├────────────────────────────────────────────┤
│ 左侧：配置筛选与供应商列表  │ 右侧：结果面板 │
│                            │               │
│                            │               │
└────────────────────────────────────────────┘
```

桌面宽度下建议比例：

- 左侧：56% ~ 62%
- 右侧：38% ~ 44%

不得让右侧结果区过窄，以免文本说明拥挤难读。

---

## 4.2 顶部状态栏

顶部区域应重构为三层信息：

### 第一层：品牌与版本

左侧：

- 应用图标
- `CC Switch Doctor`
- 小型版本号胶囊，例如 `v0.1.0`

右侧：

- `选择 DB`
- `刷新配置`
- `检查更新`

### 第二层：关键状态胶囊

仅展示几个最重要的状态：

- `DB 已连接`
- `Schema: compatible / verified / unknown`
- `CC Switch: x.y.z`（能检测到再显示）
- `无状态模式`
- `纯 HTTP`

胶囊必须短小，不允许长段说明占据工具栏。

### 第三层：简短安全说明

保留一句简洁说明：

```text
只读扫描 CC Switch 配置；不启动任何 AI CLI；不保存测试结果。
```

不再默认展示大块“安全说明卡片”占据首屏。安全说明应改为：

- 顶部一行摘要；
- 旁边一个 `查看安全边界` 按钮；
- 点击后弹出 Drawer / Modal 查看完整说明；
- 可“本次会话不再提醒”。

这样能减少首屏冗余。

---

## 4.3 左侧：供应商列表区

左侧应包含：

1. 应用维度筛选（All / Claude / Codex / Gemini ...）
2. 搜索栏
3. 批量操作区
4. 供应商列表表格或卡片列表

### 筛选条要求

应用筛选应采用 **胶囊分段按钮**，视觉上类似 CC Switch 的过滤器：

- 高度统一；
- 当前选中有明显填充背景；
- Hover 有轻微高亮；
- 可横向换行，但行间距要统一。

### 搜索区要求

搜索栏要更像产品组件：

- 左侧搜索图标；
- placeholder 更明确，例如：
  `搜索供应商 / Base URL / 模型 / Host`
- 搜索框高度与按钮一致；
- 清空搜索要有一键清除按钮。

### 批量操作按钮

按钮主次必须明确：

- `开始诊断`：主按钮
- `全选当前筛选`：次按钮
- `取消全选`：次按钮
- `仅看已勾选`：可选开关或次级按钮

当前原型中“全选当前筛选 / 取消全选”抢占了过高视觉权重，修订后必须弱化，主流程应突出“开始诊断”。

---

## 4.4 供应商列表呈现方式

### 优先方案

推荐从“硬表格”改为 **列表卡片化 + 列对齐信息** 的混合方式。

每一行/卡片至少包含：

- 左侧勾选框
- 应用名（如 `Claude Code`）
- 供应商名
- 脱敏 Key 摘要
- Base URL / Host
- 模型
- 协议标签
- 当前配置标记
- 可测试状态

### 每行高度

建议单行卡片高度 88~108 px，避免现在过于拥挤。

### 字段排版要求

#### 供应商名
- 主文字较醒目；
- Key 摘要为次级小字；
- 不允许主副信息等重。

#### Base URL
- 默认只展示 1 行；
- 超长时省略中间部分或尾部裁切；
- Hover 显示完整；
- 支持点击复制。

#### 模型与协议
- 模型作为主要内容；
- 协议使用小号次级标签，例如：
  - `Anthropic Messages`
  - `OpenAI Chat`
  - `OpenAI Responses`

#### 状态
状态不再使用过分细碎的圆圈文字，应改为语义胶囊：

- `可诊断`
- `官方配置`
- `跳过`
- `当前`
- `不可测试`

### 交互状态
每行必须有：

- Hover 背景
- Selected（勾选）背景
- Active（当前聚焦查看）边框/背景

用户点击一行时，右侧切换到该行的详细诊断卡片。

---

## 5. 右侧结果区重构

当前右侧最大问题：**像原始日志终端**。

必须改成三层结构：

### 5.1 顶部：诊断会话状态条

显示：

- 已选择数量
- 已完成 / 总数
- 当前正在诊断哪一项
- 停止按钮 / 重新开始按钮

例如：

```text
本次诊断：3 / 5 已完成 · 正在检测：DeepSeek（Claude Code）
```

### 5.2 中部：结果卡片列表

每个已诊断项使用独立结果卡片，不允许混成一整块长文本区域。

#### 结果卡片结构

1. 标题区
   - `Claude Code / DeepSeek`
   - Base URL（次级）
   - 结果状态 badge

2. 结论摘要区
   - 一句话结论
   - 置信度（high / medium / low）

3. 建议动作区
   - 明确写“建议在 CC Switch 中修改什么”
   - 不要只给技术描述

4. 成功组合区
   - Base URL
   - 协议
   - 模型
   - 端点

5. 折叠详情区
   - 尝试链
   - 原始错误
   - 请求模式
   - 测试预算占用

6. 操作区
   - `复制诊断摘要`
   - `复制建议`
   - `展开尝试链`

### 5.3 底部或折叠层：调试日志

原始日志不应该默认大面积展示。

日志区应：

- 默认折叠；
- 明确标识为“调试日志（高级）”；
- 使用等宽字体；
- 独立滚动区域；
- 默认最多显示最近若干行。

不得让普通用户第一眼看到的是控制台文本。

---

## 6. 诊断状态的视觉体系

必须建立统一状态体系：

| 状态 | 含义 | 建议颜色 |
|---|---|---|
| `CURRENT_CONFIG_OK` | 当前配置直接可用 | 绿色 |
| `PROTOCOL_FALLBACK_OK` | 原配置不通，自动变体成功 | 蓝绿色 / 青色 |
| `MODEL_FALLBACK_OK` | 模型变体成功 | 蓝色 |
| `AUTH_FAILED` | Key 无效或权限不足 | 红色 |
| `RATE_LIMITED` | 限流或余额不足 | 橙色 |
| `UNSUPPORTED_PROTOCOL` | 当前协议不兼容 | 黄色 / 橙黄色 |
| `OFFICIAL_SKIPPED` | 官方/OAuth 配置跳过 | 灰色 |
| `HOST_UNREACHABLE` | 网络不可达 | 红色 |
| `UNKNOWN_SCHEMA` | CCS Schema 无法识别 | 紫灰色 |

要求：

- 左侧列表与右侧结果使用同一套状态颜色；
- 不允许同一状态在不同组件中颜色不一致；
- Badge、边框、图标、结论文本都遵守该体系。

---

## 7. 字体与排版规范

## 7.1 字体

Windows 首发版本推荐：

```text
Inter, Segoe UI, system-ui, sans-serif
```

中文环境应保证中文字重与英文字体匹配自然。

## 7.2 字号层级

建议使用：

- 页面标题：24
- 分区标题：18~20
- 卡片标题：16~18
- 正文：13~14
- 次级文字：12~13
- 标签小字：11~12
- 等宽日志：12~13

不要出现正文过小、日志过大、标签与正文等重的问题。

## 7.3 行高

- 正文行高：1.5 ~ 1.65
- 次级说明：1.4 ~ 1.5
- 日志：1.45

## 7.4 换行规则

必须特别优化：

### URL
- 默认单行截断；
- 不要按字符硬折行破坏可读性；
- 在详情区再显示完整 URL。

### 模型名
- 支持优雅换行，但优先整词；
- 不允许把 `claude-opus-4-8` 拆得过碎。

### 错误说明
- 默认最多展示 3 行；
- 超出折叠并提供“展开更多”。

### 日志
- 保留换行，但使用等宽字体和浅灰背景；
- 支持横向滚动，不要强制乱折行。

---

## 8. 按钮系统

### 8.1 按钮层级

必须至少区分：

1. **主按钮**
   - 开始诊断
   - 重新诊断

2. **次按钮**
   - 刷新配置
   - 检查更新
   - 选择 DB
   - 全选当前筛选

3. **幽灵按钮 / 文本按钮**
   - 复制摘要
   - 展开尝试链
   - 查看原始日志

### 8.2 按钮视觉要求

- 高度统一（建议 34~38）
- 圆角统一（建议 10~12）
- Hover / Active / Disabled 态完整
- 图标与文字间距合理
- 不允许出现浏览器默认按钮样式

### 8.3 危险按钮

若有“停止测试”按钮：

- 使用中性边框按钮即可；
- 不应使用高风险红色，避免误导。

---

## 9. 间距、圆角、边框、阴影

推荐设计 token：

```text
radius-sm = 8
radius-md = 12
radius-lg = 16
spacing-1 = 4
spacing-2 = 8
spacing-3 = 12
spacing-4 = 16
spacing-5 = 20
spacing-6 = 24
```

要求：

- 页面外边距统一；
- 卡片内边距统一；
- 区块间距统一；
- 不允许出现某些卡片很挤、某些卡片过空的情况；
- 阴影要轻，边框要柔和。

---

## 10. 颜色系统

首发浅色主题建议：

- 背景：极浅灰蓝或暖灰白
- 卡片：白色或轻微染色白
- 边框：低饱和浅灰蓝
- 主色：偏蓝（参考 CC Switch）
- 成功：柔和绿色
- 警告：琥珀橙
- 失败：中度红色
- 中性：灰蓝

要求：

- 不允许高饱和大红大绿大蓝大面积铺满；
- 胶囊状态色优先采用浅底 + 彩色字/边框，而不是强对比实心块；
- 保证中文和英文在浅色背景上清晰易读。

---

## 11. 图标要求

建议使用统一图标库，如 Lucide。

常用图标：

- 数据库
- 刷新
- 更新检查
- 搜索
- 复制
- 警告
- 成功
- 失败
- 折叠/展开
- 安全说明

要求：

- 线性风格统一；
- 尺寸一致；
- 不要混用多套不一致风格图标。

---

## 12. 交互体验要求

### 12.1 首屏体验

打开应用后：

- 自动尝试定位 DB；
- 若成功，顶部显示简洁状态；
- 左侧立即可筛选和勾选；
- 右侧显示空状态插画或说明，而不是灰色大输入框。

### 12.2 空状态

右侧空状态建议文案：

```text
选择左侧配置并点击“开始诊断”，这里会展示结构化诊断结果、建议修改项与尝试链。
```

可以配一个简洁图标，不需要复杂插画。

### 12.3 运行中状态

运行中必须有：

- 总体进度条或小进度提示；
- 当前项 loading 指示；
- 左侧被诊断中的行有动态标识；
- 已完成项即时回写状态。

### 12.4 复制操作反馈

点击复制：

- 按钮文案短暂变为 `已复制`；
- 或出现小型 toast；
- 不要无反馈。

### 12.5 Toast / 提示

全局 toast 样式要统一：

- 成功 / 警告 / 错误 / 信息；
- 自动消失；
- 不遮挡主要操作区。

---

## 13. “日志”与“尝试链”的表现要求

当前最大问题之一是日志太像“糊在页面上的输出”。

必须改成：

### 尝试链（给普通用户）

采用结构化条目：

```text
1. 当前配置
   Anthropic Messages / grok-4.5 / https://sub.example.com/v1/messages
   结果：403 PERMISSION_DENIED

2. 协议回退
   OpenAI Chat Completions / grok-4.5 / https://sub.example.com/v1/chat/completions
   结果：成功
```

### 原始日志（给高级用户）

采用折叠面板：

- 标题：`调试日志（高级）`
- 内容：等宽字体
- 支持复制
- 默认隐藏

不得把尝试链和原始日志混在一起。

---

## 14. 文案质量要求

AI 生成的中文界面经常会出现“翻译腔”和“工程腔”，必须约束。

### 文案原则

- 简洁；
- 面向用户；
- 不堆砌术语；
- 有结论再有细节。

### 错误示例

```text
诊断状态：UNSUPPORTED_PROTOCOL。请查看尝试链与错误摘要。
```

### 更好的写法

```text
当前协议不兼容。
该供应商未能按当前配置成功响应；已尝试多种兼容组合，但暂未发现稳定可用方案。
可展开“尝试链”查看详细过程。
```

要求：

- UI 上允许保留机器状态码，如 `UNSUPPORTED_PROTOCOL`，但必须配中文解释；
- 对用户展示的文案优先中文自然表达；
- 避免整屏只有大写枚举名。

---

## 15. 可访问性与易用性

即使是内部工具，也要做到基础可用性：

- 键盘可操作；
- 焦点态清晰；
- 按钮 aria label 基本完整；
- 颜色不应是唯一状态区分手段；
- 对比度满足基本可读性；
- 主要内容支持文本选择与复制。

---

## 16. 响应式与窗口行为

Windows 桌面应用不要求移动端适配，但要支持：

- 最小窗口尺寸合理（例如 1200 x 760）；
- 缩小时布局不崩；
- 左右列可按断点适当收缩；
- 极窄尺寸时右侧结果区仍可读；
- 长列表和长结果区分别独立滚动。

不得出现整个页面共用一个大滚动条，导致操作体验糟糕。

---

## 17. 首版实现建议（给 AI）

为保证一次 Goal 成功，首版建议采用：

- React + TypeScript
- Tailwind CSS 或等价 token 化方案
- shadcn/ui 风格组件（可借鉴，不要求全量接入）
- Lucide 图标
- 简洁动画（不追求复杂动效）

要求 AI：

- 建立统一 design tokens；
- 抽离公共组件：Badge、Button、SearchInput、ResultCard、ProviderRow、StatusChip、EmptyState、LogPanel；
- 先搭完整 UI 框架，再接入数据；
- 避免“功能先写完，样式最后随便补一下”的做法。

---

## 18. 新增验收标准（强制）

除了原功能验收外，新增以下 UI/UX 验收项：

### A. 视觉一致性
- 至少 90% 主要控件不出现浏览器默认样式；
- 按钮、输入框、卡片、badge、tab 风格统一；
- 圆角、边框、阴影、间距有统一 token；
- 页面整体明显具备产品感，而非调试页感。

### B. 可读性
- 左侧列表在 1366x768 下可清晰阅读；
- URL、模型、Key 摘要不会难看乱折行；
- 右侧结果卡片中，结论、建议、尝试链层级清晰；
- 日志默认不喧宾夺主。

### C. 结构化结果
- 每个结果项必须卡片化展示；
- 必须包含“结论摘要 + 建议动作 + 成功组合/失败原因 + 尝试链”；
- 原始日志默认折叠。

### D. 风格贴近 CC Switch
- 顶部工具栏、筛选 chip、卡片和 badge 风格需明显参考 CC Switch；
- 用户第一眼应能感受到“像 CC Switch 的配套工具”。

### E. 交互体验
- 复制按钮有反馈；
- 诊断运行有进度反馈；
- 空状态友好；
- 不因数据多而页面拥挤凌乱。

---

## 19. 对 AI 开发执行的补充指令

把以下要求直接加到开发 Prompt 中：

```text
前端界面不能停留在工程原型层级，必须做产品化美化。整体视觉与交互风格需明显参考 CC Switch：包括顶部状态栏、筛选胶囊、卡片、按钮、状态标签、间距、字体、日志折叠方式与结果卡片布局。不得交付一个“能用但像调试页面”的 UI。

重点提升：
1. 页面总体布局；
2. 按钮主次；
3. 左侧供应商列表可读性；
4. 右侧诊断结果结构化卡片；
5. 尝试链与原始日志的分离；
6. 字体、换行、间距、颜色系统；
7. 与 CC Switch 家族风格的一致性。

验收时必须提供若干桌面端截图，证明界面已完成产品化美化，而不是只有功能实现。
```

---

## 20. 结论

本补充规范是 **强制性增强要求**。

如果 AI 只完成功能，但界面仍然接近当前截图中的粗糙原型，则视为 **未达到交付标准**。

交付目标应为：

> 一个功能正确、安全边界明确、并且视觉上明显接近 CC Switch 生态风格的桌面诊断工具。

